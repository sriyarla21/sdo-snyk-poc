version: 2
filters:
  - id: fail-on-critical-vulns
    # Check for vulnerabilities with severity "critical"
    jq: '.vulnerabilities[] | select(.severity == "critical")'
    # If any critical vulnerabilities are found, the build will fail (exit code 1)
